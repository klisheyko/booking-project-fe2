import React from 'react';
import SiteName from "./SiteName";
import Menu from "./Menu";

const Header = () => {
  return (
    <div>
      <SiteName/>
      <Menu/>
    </div>
  );
};

export default Header;
